# The app is built by Vite + Nitro using Nitro's "netlify" preset (see vite.config.ts).
# That preset emits client/static assets to `dist` and the SSR handler to
# `.netlify/functions-internal/server`, which Netlify picks up automatically.
[build]
  command = "npm run build"
  publish = "dist"
